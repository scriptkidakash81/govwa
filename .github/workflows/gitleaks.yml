name: Gitleaks

# Run workflow each time code is pushed to your repository.
on:
  pull_request:
    branches:
      - main
jobs:
 Gitleaks-run:
  uses: scriptkidakash81/git-central/workflows/gitleaks.yml@main
  secrets: inherit
